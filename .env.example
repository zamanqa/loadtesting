# API Configuration — used by both Cypress and k6
API_BASE_URL=https://hub-api-development-680576524870.europe-west3.run.app
BASE_URL=https://hub-api-development-680576524870.europe-west3.run.app
API_VERSION=2026-04
CONSUMER_KEY=ck_shopify_po
CONSUMER_SECRET=cs_i2451dlc5lkcsgww0gks

# Company
COMPANY_ID=734f-4c766638po

# k6 tuning — optional overrides (defaults shown)
REQUEST_TIMEOUT=10s    # used by load and soak tests
STRESS_TIMEOUT=20s     # used by stress test (doubled — server slows at 150 VUs)
PER_PAGE=100           # page size for list/filter requests (smoke and spike always use 10)

# Grafana k6 Cloud — required for :cloud scripts
K6_CLOUD_TOKEN=            # from Grafana Cloud → k6 → Settings → API Token
K6_CLOUD_STACK=            # your Grafana stack URL, e.g. https://yourorg.grafana.net

# Database Configuration — Cypress cy.task('queryDb') only
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_NAME=
DB_PORT=